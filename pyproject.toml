[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "coal-docs"
version = "1.0.0"
description = "Coal language documentation with Shiki syntax highlighting"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["coal_shiki"]
